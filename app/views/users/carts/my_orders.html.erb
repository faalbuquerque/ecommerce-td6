<h1>Meus Pedidos</h1>

<% @carts.each do |cart| %>
  <ul>
    <li><%= link_to cart.product.name, order_users_cart_path(cart), class:"btn btn-success btn-sm mb-1 mt-1" %></li>
    <li>Data da compra <%= cart.created_at.strftime("%d/%m/%Y") %></li>
  </ul>
<% end %>
